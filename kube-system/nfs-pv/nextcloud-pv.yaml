---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-nextcloud-pv
  namespace: kube-system
spec:
  storageClassName: nextcloud
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 10.10.10.30
    path: "/mnt/tardisk/nextcloud"
  mountOptions:
    - nfsvers=4.1
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-nextcloud-pvc
  namespace: kube-system
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nextcloud
  resources:
    requests:
      storage: 1Mi

dashboard:
  loadBalancerIP: $EXTERNAL_IP 
  domain: t.k.$DOMAIN
  auth:
    basic:
       $TRAEFIK_AUTH
  ingress:
    tls:
      - secretName: acme-crt-secret
        hosts:
          - t.k.$DOMAIN

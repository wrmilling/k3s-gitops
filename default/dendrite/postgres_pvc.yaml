---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dendrite-postgres-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
  storageClassName: rook-ceph-block
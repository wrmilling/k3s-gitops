---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: statping-ng-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: rook-ceph-block

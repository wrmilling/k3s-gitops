---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant-postgres
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.bitnami.com/bitnami
      chart: postgresql
      version: 11.9.13
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: postgres
      tag: 15.4@sha256:9abf609db8150db07d247fc5fe7457a8fbbccb03f650af6ed2ff338e13d7d9d1
    postgresqlDataDir: "/data/pgdata"
    primary:
      persistence:
        enabled: true
        existingClaim: hass-postgres-pvc
        mountPath: "/data/"
      tolerations:
      - key: "arm"
        operator: "Exists"
      podAnnotations:
        backup.velero.io/backup-volumes: data
    auth:
      enablePostgresUser: false
      username: home-assistant
      database: home-assistant
      password: ${SECRET_HASS_DB_PASSWORD}
      postgresqlPassword: ${SECRET_HASS_DB_ADMIN_PASSWORD}
    shmVolume:
      enabled: false
    volumePermissions:
      enabled: false
    resources:
      limits:
        memory: 4000Mi
      requests:
        memory: 2000Mi

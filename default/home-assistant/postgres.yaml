---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant-postgres
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.bitnami.com/bitnami
      chart: postgresql
      version: 11.9.13
      sourceRef:
        kind: HelmRepository
        name: bitnami-charts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: postgres
      tag: 14.9@sha256:3e0e1563997c2af5d88dc361d9cde7dc4119f3bf9da6e9141d76c8bb5caa270d
    postgresqlDataDir: "/data/pgdata"
    primary:
      persistence:
        enabled: true
        existingClaim: hass-postgres-pvc
        mountPath: "/data/"
      tolerations:
      - key: "arm"
        operator: "Exists"
      podAnnotations:
        backup.velero.io/backup-volumes: data
    auth:
      enablePostgresUser: false
      username: home-assistant
      database: home-assistant
      password: ${SECRET_HASS_DB_PASSWORD}
      postgresqlPassword: ${SECRET_HASS_DB_ADMIN_PASSWORD}
    shmVolume:
      enabled: false
    volumePermissions:
      enabled: false
    resources:
      limits:
        memory: 4000Mi
      requests:
        memory: 2000Mi

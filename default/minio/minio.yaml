---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: minio
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://charts.min.io/
      chart: minio
      version: 5.0.13
      sourceRef:
        kind: HelmRepository
        name: minio-charts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: quay.io/minio/minio
      tag: RELEASE.2023-09-07T02-05-02Z@sha256:7e697b900f60d68e9edd2e8fc0dccd158e98938d924298612c5bbd294f2a1e65
      pullPolicy: IfNotPresent
    mcImage:
      repository: quay.io/minio/mc
      tag: RELEASE.2023-01-11T03-14-16Z@sha256:1f374e2f61a8a4902ed528fb1d19f93a44b3d81a158b003779f85883833990c3
      pullPolicy: IfNotPresent
    mode: standalone
    rootUser: ${SECRET_MINIO_ACCESS_KEY}
    rootPassword: ${SECRET_MINIO_SECRET_KEY}
    users:
    - accessKey: ${SECRET_THANOS_S3_ACCESS_KEY}
      secretKey: ${SECRET_THANOS_S3_SECRET_KEY}
      policy: readwrite
    persistence:
      enabled: true
      existingClaim: "nfs-minio-pvc"
    ingress:
      enabled: false
      ingressClassName: nginx-internal
      hosts:
        - minio.k.${SECRET_DOMAIN}
      tls:
        - secretName: minio-cert
          hosts:
            - minio.k.${SECRET_DOMAIN}
    consoleIngress:
      enabled: true
      hosts:
        - mc.k.${SECRET_DOMAIN}
      tls:
        - secretName: minio-console-cert
          hosts:
            - mc.k.${SECRET_DOMAIN}
    securityContext:
      enabled: false
    resources:
      requests:
        memory: 350Mi
        cpu: 25m
      limits:
        memory: 1000Mi
    tolerations:
      - key: "arm"
        operator: "Exists"
    metrics:
      serviceMonitor:
        enabled: true

---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/operator.victoriametrics.com/vmservicescrape_v1beta1.json
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMServiceScrape
metadata:
  name: mastodon-garage
  namespace: social
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: mastodon-garage
      app.kubernetes.io/instance: mastodon-garage
  namespaceSelector:
    matchNames:
      - social
  endpoints:
    - port: admin
      path: /metrics
      honorLabels: true
